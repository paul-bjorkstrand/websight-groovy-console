import { colors } from 'websight-admin/theme';

import { GROOVY_CONSOLE_ROOT_PATH } from '../../../../utils/GroovyConsoleConstants.js';

const NavigationItemFragment = {
    title: 'Groovy Console',
    img: 'code',
    color: colors.green,
    description: 'Develop Groovy scripts to automate repetitive tasks. Create, test and promote without mistakes.',
    href: GROOVY_CONSOLE_ROOT_PATH
}

export default NavigationItemFragment;